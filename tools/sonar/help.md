Launch: Run docker compose up -d in the same directory as the file.
Access: Open http://localhost:9000 once the startup is complete.
Authentication: Use the default admin / admin credentials.

DB Details:
SONAR_JDBC_URL=jdbc:postgresql://db:5432/sonar
SONAR_JDBC_USERNAME=sonar
SONAR_JDBC_PASSWORD=sonar

Github:
thararaoganji
