package portal.expenses.entity;

public enum PolicyRuleType {
    AMOUNT_THRESHOLD,
    CATEGORY_RULE,
    AGE_LIMIT,
    RECEIPT_REQUIRED,
    AUTO_APPROVE,
    MANAGER_APPROVAL,
    FINANCE_APPROVAL
}
